<%= form_with(model: job, local: true) do |form| %>
  <% if job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(job.errors.count, "error") %> prohibited this job from being saved:</h2>

      <ul>
      <% job.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
 <%= form.collection_select :jobtype_id, Jobtype.all, :id, :name , required: true %>
  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :job_title , class: 'form-control' , required: true %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :job_description , class: 'form-control' , required: true %>
  </div>
  <div class="field">
    <%= form.label :jobcity %>
      <%= form.text_field :jobcity, id: :jobcity , class: 'form-control' , required: true %>
  </div>
  <div class="field">
    <%= form.label :urgent %>
    <%= form.check_box :urgent, id: :job_urgent %>
  </div>
 <%= form.file_field :companylogo  %>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
